query launchItemDetails($id: ID!) {
    launch(id: $id) {
        id
        details
        launch_date_utc
        launch_site {
            site_id
            site_name
        }
        launch_success
        links {
            article_link
            flickr_images
            mission_patch
            video_link
        }
        mission_name
    }
}